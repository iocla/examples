
## x86_64 differences 

The AMD64 architecture is an extension of x86 instruction set to enable 64-bit computing while remaining compatible with existing x86 software. The CPU can operate in 64-bit mode, where semantic of several x86 instructions has been changed.


1. The Calling Convention (System V AMD64 ABI)
  Unlike 32-bit assembly where arguments are passed on the stack, x86_64 uses registers.
   * Integer/Pointer Arguments: Passed in rdi, rsi, rdx, rcx, r8, and r9 (in that order).
   * Return Value: Placed in rax
   * Functions can use: rax, rcx, rdx, rsi, rdi, r8, r9, r10, r11, ST0-ST7, K0-K7, XMM0-XMM15, YMM0-YMM15, ZMM0-ZMM31
   * Functions should preserve: rbx, rbp, r12, r13, r14, or r15 
   * DF in the RFLAGS register must be clear (set to “forward”
direction) on function entry and return.[AMD64-PSABI]


2. Stack Alignment
  The stack pointer (rsp) must be aligned to a 16-byte boundary before calling any other function (including printf from libc).
   * The call instruction pushes an 8-byte return address onto the stack, so upon entry to a function, the stack is usually "misaligned" by 8 bytes. You often need to subtract another 8 from rsp to realign it before making further calls. [Agner-Calling]
   * red zone: The 128-byte area beyond the location pointed to by rsp is considered to be reserved and shall not be modified by signal or interrupt handlers. Therefore, functions may use this area for temporary data that is not needed across function calls. In particular, leaf functions may use this area for their entire stack frame, rather than adjusting the stack pointer in the prologue and epilogue. 


3. Register Sizes
   * r8 - r15 registers:   
    - rXb for 8 bit register ( LSByte of the 8 byte value)
    - rXw for 16 bits
    - rXd for 32 bits
    - rX  for 64 bits
Where X stands for integer in the range of 8 to 15.
   * Moving a value into a 32-bit register (e.g., mov eax, 1)   automatically zeros the upper 32 bits of the corresponding 64-bit register (rax). 
     - `imul ebx` erases upper part of rax
     - `mov eax, esi` erases upper part of rax
     - 8-bit and 16-bit operands generate an 8-bit or 16-bit result. The upper 56 bits or 48 bits (respectively) of the destination general-purpose register are not modified by the operation!


4. System Calls
  Use the syscall instruction instead of the legacy int 0x80.
   * Syscall Number: rax
   * Arguments: rdi, rsi, rdx, r10, r8, r9 


5. Addressing 
 - 64 bit immediates \
   `mov  rsi, [0x0123456789abcdef]` \
         doesn't work, you need \
   `mov rax, 0x0123456789abcdef` \
   `mov rsi, [rax]` \
   RISC style, and loading the immediate generates a `movabs` instruction in objdump and godbolt. 
 - Single byte encoding of inc and dec instructions is no longer available. Instead the opcodes are used to encode a new prefix REX with four one-bit arguments. First argument is used to overwrite instruction operand size into 64 bits. Other three are used to increase amount of general purpose registers from 8 to 16.
 - New 64-bit addressing mode is used by default. Prefix is available to overwrite into 32-bit addressing when needed.
 - One of multiple possible encodings of direct addressing has been changed into instruction pointer relative addressing. Instruction pointer relative addressing is now one byte shorter than direct addressing.
 - Default operand size remains 32-bit, however stack manipulation instructions, such as push and pop defaults to 64-bit operand size.
 - The immediate operands of instructions has not been extended to 64 bits to keep instruction size smaller, instead they remain 32-bit sign extended. Additionally the movabs instruction to load arbitrary 64-bit constant into register and to load/store integer register from/to arbitrary constant 64-bit address is available.
 - Several new instructions have been added to allow 64-bit conversions of data types.
 - Unlike earlier 64-bit architectures GCC has been ported to, some AMD64 features are unique, such as CISC instruction set, generally usable IP relative addressing, partial support for 64-bit immediate operands and more.

6. RIP relative addressing
  - X86-64 defines a new instruction pointer relative addressing mode to simplify writing of position independent code.


7. Floating point 
 - 8 x87 floating point registers, each 80 bits wide ST0 - ST7. Each of the x87 floating point registers may be referred to in MMX mode as a 64-bit register. All of these registers are global to all procedures active for a given thread.
 - 16 256-bit wide AVX registers (ymm0 - ymm15). The lower 128-bits of ymm0 - ymm15 are aliased to the respective 128b-bit SSE registers (xmm0 - xmm15).
 - The MM0-MM7 registers are aliased on the lower 64 bits of the floating point x87 registers ST0-ST7 
 - The CPU shall be in x87 mode upon entry to a function. Therefore, every function that uses the MMX registers is required to issue an emms or femmsinstruction after using MMX registers, before returning or calling another function.
 - The control bits of the MXCSR register are callee-saved (preserved across calls), while the status bits are caller-saved (not preserved). The x87 status word register is caller-saved, whereas the x87 control word is callee-saved.
 - 

\
**[AMD64-PSABI]** _UNIX System V Application Binary Interface; AMD64 Architecture Processor Supplement_, Draft, (Ed. J. Hubicka, A. Jaeger, M. Mitchell), https://refspecs.linuxbase.org/elf/x86_64-abi-0.99.pdf, (2019) \
\
**[Agner-Calling]** _Calling conventionsfor different C++ compilers and operating systems_,  Agner Fog. Technical University of Denmark, 2023. https://agner.org/optimize/calling_conventions.pdf#page=2.06

